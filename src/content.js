import firstFile from './firstFile';
import secondFile from './secondFile';

const content = [
    {
        key: 'about',
        content: firstFile,
    },
    {
        key: 'support',
        content: secondFile
    }
]

export default content;